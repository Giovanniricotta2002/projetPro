apiVersion: v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      initContainers:
        